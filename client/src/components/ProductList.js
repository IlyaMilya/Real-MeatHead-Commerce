import React from "react";


function ProductList({products}) {

//   const productItems = products.map((product) => {
//     return <Product key={product.id} id={product.id} category={product.category}name={product.name} price={product.price} image={product.image} company={product.company} />
//   })

//   return (
//     <>
//         {productItems}
//     </>
//   );
// 
}

export default ProductList;